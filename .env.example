
# Firebase Configuration
# Copy this file to .env.local and fill in your actual Firebase project values
# You can find these values in your Firebase console under Project Settings > General

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Instructions:
# 1. Copy this file and rename it to .env.local
# 2. Replace all the placeholder values with your actual Firebase configuration
# 3. Do NOT commit .env.local to version control - it's already in .gitignore
# 4. Restart your development server after creating or modifying .env.local

# Note: All these values MUST be filled in for Firebase to work correctly
# If you're seeing connection errors, double check these values against your Firebase Console
